-- Verificar la relación en la tabla Asignacion
SELECT * FROM Asignacion;

-- Verificar la relación en la tabla Bancos
SELECT * FROM Bancos;

-- Verificar la relación en la tabla Clientes
SELECT * FROM Clientes;

-- Verificar la relación en la tabla Colaborador
SELECT * FROM Colaborador;

-- Verificar la relación en la tabla Comuna
SELECT * FROM Comuna;

-- Verificar la relación en la tabla Datos_Bancarios
SELECT * FROM Datos_Bancarios;

-- Verificar la relación en la tabla Estado_Asignacion
SELECT * FROM Estado_Asignacion;

-- Verificar la relación en la tabla Estado_Cliente
SELECT * FROM Estado_Cliente;

-- Verificar la relación en la tabla Estado_Colaborador
SELECT * FROM Estado_Colaborador;

-- Verificar la relación en la tabla Tipo_cuenta
SELECT * FROM Tipo_cuenta;
